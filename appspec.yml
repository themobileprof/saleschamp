version: 0.0
os: linux
hooks:
  AfterInstall:
    - location: scripts/pull-app.sh
      runas: ubuntu
      timeout: 300
  ApplicationStart:
    - location: scripts/restart-app.sh
      runas: ubuntu
      timeout: 60
